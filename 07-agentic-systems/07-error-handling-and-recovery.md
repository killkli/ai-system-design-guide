# 錯誤處理與恢復

代理以非確定性方式失敗。錯誤處理已從「Try-Catch 區塊」演進至**代理自我修正**與**有狀態回滾**，LangGraph 與 Microsoft Agent Framework 等框架提供原生檢查點/恢復原語。

## 目錄

- [代理失敗的分類學](#fail-types)
- [自我修正迴圈](#correction)
- [有狀態回滾（檢查點）](#rollbacks)
- [「困在迴圈中」的修復](#stuck)
- [優雅降級](#degradation)
- [面試問題](#interview-questions)
- [參考文獻](#references)

---

## 代理失敗的分類學

1. **幻覺工具**：呼叫不存在的工具。
2. **結構描述違規**：將錯誤引數傳遞給真實工具。
3. **環境錯誤**：工具存在，但外部 API 離線。
4. **邏輯停滯**：代理重複執行相同的失敗動作（ReAct 死亡迴圈）。

---

## 自我修正迴圈

錯誤現在被視為**資訊 token**。

- **模式**：當工具失敗時，錯誤訊息*不僅*被記錄；它作為提示回饋給模型：*「動作失敗，錯誤：X。反思為何發生，並提供替代策略。」*
- **推理模型**（Claude Opus 4.7 延伸思考、GPT-5.5 推理、DeepSeek-R2）：這些模型在這方面表現出色，因為它們在隱藏思維鏈中「內化」錯誤，帶來更高的單次恢復率。

---

## 有狀態回滾（檢查點）

對於長期執行的代理，步驟 9 中的錯誤不應使整個專案崩潰。

- **檢查點**：使用 LangGraph 或類似的高可靠性系統在每個成功工具呼叫後將「狀態快照」儲存至資料庫。
- **回滾**：如果代理進入邏輯停滯，監督者代理可以**將通用狀態重設**至步驟 5——最後一個「安全」狀態——並強制走向不同路徑。

---

## 「困在迴圈中」的修復

無限迴圈是代理系統中的頭號成本沉沒。

**解決方案**：**計數器干預**。
1. 如果在同一工作階段中看到相同的 `(Tool, Args)` 元組 3 次，編排者中斷模型。
2. 它注入一個強制**「樞軸指令」**：*「你已經嘗試搜尋 'X' 三次。此路徑已死。你必須嘗試不同工具或承認你被困住了。」*

---

## 優雅降級

如果高推理代理（Claude Opus 4.7、GPT-5.5 推理）持續失敗，我們降級至：
- **簡化代理**：較小模型，工具較少但更可靠。
- **僅 RAG 模式**：停用動作，僅根據知識庫提供概念性答案。
- **人類升級**： （見下一章）。

---

## 面試問題

### Q：為什麼傳統的「例外處理」（Try/Catch）對代理系統不足？

**理想回答：**
在傳統軟體中，例外是「停止」命令。在代理系統中，模型是「驅動者」。如果系統只是停止，使用者任務就失敗了。我們使用**錯誤注入**而非例外處理。我們在平台層級攔截例外並將其轉換為**綜合觀察**饋給模型。這讓模型能夠「推理」失敗周圍的情況。TRY/Catch 只修復程式碼；錯誤注入讓模型修復**計畫**。

### Q：如何處理「靜默失敗」（工具回傳 200 OK 但資料錯誤）？

**理想回答：**
靜默失敗是最危險的。我們實施**輸出驗證代理**。對於關鍵步驟，我們不僅僅接受工具輸出。我們將輸出傳遞給一個「驗證代理」（通常是一個較小較快的模型），其唯一工作是檢查：*「這個工具輸出實際上回答了提供的查詢了嗎？」* 如果驗證者說「否」，它會像硬錯誤一樣觸發自我修正迴圈。

---

## 參考文獻

- LangGraph. 《持久化與檢查點》（2025）
- Shinn et al. 《從錯誤中學習》（2024 更新）
- Microsoft. 《管理代理系統中的幻覺》（2025）

---

*下一篇：[人類在迴圈中模式](08-human-in-the-loop-patterns.md)*
